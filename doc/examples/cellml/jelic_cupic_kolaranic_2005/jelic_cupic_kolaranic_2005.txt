//Created by libAntimony v1.4

//Warnings from automatic translation:
//    The units of "tau" ('dimensionless') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "tau" ('dimensionless') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.

model jelic_cupic_kolaranic_2005__environment(time_, tau, C_0)

  // Assignment Rules:
  time_ := tau / C_0;
end

model jelic_cupic_kolaranic_2005__a(a, alpha, beta, K, g, tau, C_1)

  // Assignment Rules:
  A := C_1 * a;

  // Variable initializations:
  a = 7.68e-8;
end

model jelic_cupic_kolaranic_2005__g(g, alpha, gamma, L, a, tau, C_2)

  // Assignment Rules:
  G := C_2 * g;

  // Variable initializations:
  g = 3.43e-8;
end

model jelic_cupic_kolaranic_2005__reaction_constants(alpha, beta, gamma, L, K, C_0, C_1, C_2, time_)

  // Assignment Rules:
  alpha := k3 / k2;
  beta := k6 / k2;
  gamma := k7 / k2;
  L := root(power(km, 2 ) * k4 / power(k2, 3 ));
  K := root(power(k0, 2 ) * k4 / power(k2, 3 ));
  C_0 := k2;
  C_1 := root(k2 / k4);
  C_2 := root(k2 / k4);

  // Variable initializations:
  k2 = 6e-4;
  k3 = 0.0000048;
  k6 = 0.000891;
  k7 = 0.006831;
  k0 = 8.7831e-11;
  k4 = 2.1e12;
  km = 6.9001e-14;
end

model jelic_cupic_kolaranic_2005____main()

  // Sub-modules, and any changes to those submodules:
  environment: jelic_cupic_kolaranic_2005__environment(time_, tau, C_0);
  a: jelic_cupic_kolaranic_2005__a(a0, alpha, beta, K, g0, tau, C_1);
  g: jelic_cupic_kolaranic_2005__g(g0, alpha, gamma, L, a0, tau, C_2);
  reaction_constants: jelic_cupic_kolaranic_2005__reaction_constants(alpha, beta, gamma, L, K, C_0, C_1, C_2, time_);
end
