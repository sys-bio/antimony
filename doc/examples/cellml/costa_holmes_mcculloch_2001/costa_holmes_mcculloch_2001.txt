//Created by libAntimony v1.3
model guccione_mcculloch_waldman_1991_version02__equations(E11, E22, E33, E12, E13, E23, a, bff, bfn, bfs, bnn, bns, bss, Tdev11, Tdev22, Tdev33, Tdev12, Tdev13, Tdev23)

  // Assignment Rules:
  Tdev11 := a * bff * E11 * exp(q);
  Tdev22 := a * bss * E22 * exp(q);
  Tdev33 := a * bnn * E33 * exp(q);
  Tdev12 := a * bfs * E12 * exp(q);
  Tdev13 := a * bfn * E13 * exp(q);
  Tdev23 := a * bns * E23 * exp(q);
  q := bff * power(E11, 2 ) + bss * power(E22, 2 ) + bnn * power(E33, 2 ) + 2 * bfn * power(E13, 2 ) + 2 * bfs * power(E12, 2 ) + 2 * bns * power(E23, 2 );
end

model guccione_mcculloch_waldman_1991_version02__interface(E11, E12, E13, E22, E23, E33, a, bff, bfn, bfs, bnn, bns, bss, Tdev11, Tdev12, Tdev13, Tdev22, Tdev23, Tdev33)

  // Sub-modules, and any changes to those submodules:
  equations: guccione_mcculloch_waldman_1991_version02__equations(E11, E22, E33, E12, E13, E23, a, bff, bfn, bfs, bnn, bns, bss, Tdev11, Tdev22, Tdev33, Tdev12, Tdev13, Tdev23);

  // Variable initializations:
  a = 0;
  bff = 0;
  bfn = 0;
  bfs = 0;
  bnn = 0;
  bns = 0;
  bss = 0;
end

model guccione_mcculloch_waldman_1991_version02____main()

  // Sub-modules, and any changes to those submodules:
  interface: guccione_mcculloch_waldman_1991_version02__interface(E11, E12, E13, E22, E23, E33, a, bff, bfn, bfs, bnn, bns, bss, Tdev11, Tdev12, Tdev13, Tdev22, Tdev23, Tdev33);
end
