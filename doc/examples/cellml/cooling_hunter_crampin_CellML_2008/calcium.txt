//Created by libAntimony v1.4
model calcium__calcium(t, Ca, J_gain_Ca)

  // Rate Rules:
  Ca' = J_gain_Ca;

  // Variable initializations:
  Ca = 0.100;
end

model calcium____main()

  // Sub-modules, and any changes to those submodules:
  calcium: calcium__calcium(t, Ca, J_gain_Ca);
end
