//Created by libAntimony v1.4
model olsen_2003__environment(time_)
end

model olsen_2003__O2(O2, v1, v5, v7, v11, v13_back, v13, time_)

  // Rate Rules:
  O2' = -(v1) - v5 + v7 - v11 + v13 - v13_back;

  // Variable initializations:
  O2 = 5.33e-6;
end

model olsen_2003__O2_radical(O2_radical, v5, v6, v7, time_)

  // Rate Rules:
  O2_radical' = v5 - v6 - 2 * v7;

  // Variable initializations:
  O2_radical = 0.026e-6;
end

model olsen_2003__NADH(NADH, v1, v12, v14, time_)

  // Rate Rules:
  NADH' = -(v1) + v12 - v14;

  // Variable initializations:
  NADH = 0.95e-6;
end

model olsen_2003__NAD(NAD, v1, v5, v8, v10, time_)

  // Rate Rules:
  NAD' = v1 + v5 + v8 + v10;

  // Variable initializations:
  NAD = 558e-6;
end

model olsen_2003__NAD_radical(NAD_radical, v9, v5, v8, v10, v14, time_)

  // Rate Rules:
  NAD_radical' = -(2 ) * v9 - v5 - v8 - v10 + v14;

  // Variable initializations:
  NAD_radical = 5e-10;
end

model olsen_2003__NAD2(NAD2, v9, time_)

  // Rate Rules:
  NAD2' = v9;

  // Variable initializations:
  NAD2 = 0.12e-6;
end

model olsen_2003__H2O2(H2O2, v1, v2, v7, time_)

  // Rate Rules:
  H2O2' = v1 - v2 - v7;

  // Variable initializations:
  H2O2 = 0.013e-6;
end

model olsen_2003__Per3(Per3, v2, v4, v6, v10, time_)

  // Rate Rules:
  Per3' = -(v2) + v4 - v6 - v10;

  // Variable initializations:
  Per3 = 0.059e-6;
end

model olsen_2003__Per2(Per2, v10, v11, time_)

  // Rate Rules:
  Per2' = -(v11) + v10;

  // Variable initializations:
  Per2 = 1e-10;
end

model olsen_2003__coI(coI, v2, v3, v8, time_)

  // Rate Rules:
  coI' = v2 - v3 + v8;

  // Variable initializations:
  coI = 8.9e-10;
end

model olsen_2003__coII(coII, v3, v4, time_)

  // Rate Rules:
  coII' = v3 - v4;

  // Variable initializations:
  coII = 0.026e-6;
end

model olsen_2003__coIII(coIII, v6, v8, v11, time_)

  // Rate Rules:
  coIII' = v6 - v8 + v11;

  // Variable initializations:
  coIII = 1.31e-6;
end

model olsen_2003__Ar_radical(Ar_radical, v3, v4, v14, time_)

  // Rate Rules:
  Ar_radical' = v3 + v4 - v14;

  // Variable initializations:
  Ar_radical = 0.12e-6;
end

model olsen_2003__ArH(ArH, v3, v4, v14, time_)

  // Rate Rules:
  ArH' = -(v3) - v4 + v14;

  // Variable initializations:
  ArH = 299.88e-6;
end

model olsen_2003__v1(v1, NADH, O2)

  // Assignment Rules:
  v1 := k1 * NADH * O2;

  // Variable initializations:
  k1 = 3;
end

model olsen_2003__v2(v2, H2O2, Per3)

  // Assignment Rules:
  v2 := k2 * H2O2 * Per3;

  // Variable initializations:
  k2 = 1.8e7;
end

model olsen_2003__v3(v3, coI, ArH)

  // Assignment Rules:
  v3 := k3 * coI * ArH;

  // Variable initializations:
  k3 = 1.5e5;
end

model olsen_2003__v4(v4, coII, ArH)

  // Assignment Rules:
  v4 := k4 * coII * ArH;

  // Variable initializations:
  k4 = 5.2e3;
end

model olsen_2003__v5(v5, NAD_radical, O2)

  // Assignment Rules:
  v5 := k5 * NAD_radical * O2;

  // Variable initializations:
  k5 = 2e7;
end

model olsen_2003__v6(v6, O2_radical, Per3)

  // Assignment Rules:
  v6 := k6 * O2_radical * Per3;

  // Variable initializations:
  k6 = 1.7e7;
end

model olsen_2003__v7(v7, O2_radical)

  // Assignment Rules:
  v7 := k7 * power(O2_radical, 2 );

  // Variable initializations:
  k7 = 2e7;
end

model olsen_2003__v8(v8, coIII, NAD_radical)

  // Assignment Rules:
  v8 := k8 * coIII * NAD_radical;

  // Variable initializations:
  k8 = 4e7;
end

model olsen_2003__v9(v9, NAD_radical)

  // Assignment Rules:
  v9 := k9 * power(NAD_radical, 2 );

  // Variable initializations:
  k9 = 6e7;
end

model olsen_2003__v10(v10, NAD_radical, Per3)

  // Assignment Rules:
  v10 := k10 * Per3 * NAD_radical;

  // Variable initializations:
  k10 = 1.8e6;
end

model olsen_2003__v11(v11, Per2, O2)

  // Assignment Rules:
  v11 := k11 * Per2 * O2;

  // Variable initializations:
  k11 = 1e5;
end

model olsen_2003__v12(v12)

  // Assignment Rules:
  v12 := k12;

  // Variable initializations:
  k12 = 0.08e-6;
end

model olsen_2003__v13(v13)

  // Assignment Rules:
  v13 := k13 * O2eq;

  // Variable initializations:
  O2eq = 1.2e-5;
  k13 = 6e-3;
end

model olsen_2003__v13_back(v13_back, O2)

  // Assignment Rules:
  v13_back := k13_ * O2;

  // Variable initializations:
  k13_ = 6e-3;
end

model olsen_2003__v14(v14, Ar_radical, NADH)

  // Assignment Rules:
  v14 := k14 * Ar_radical * NADH;

  // Variable initializations:
  k14 = 7e5;
end

model olsen_2003____main()

  // Sub-modules, and any changes to those submodules:
  environment: olsen_2003__environment(time_);
  O2: olsen_2003__O2(O20, v15, v50, v70, v110, v13_back0, v130, time_);
  O2_radical: olsen_2003__O2_radical(O2_radical0, v50, v60, v70, time_);
  NADH: olsen_2003__NADH(NADH0, v15, v120, v140, time_);
  NAD: olsen_2003__NAD(NAD0, v15, v50, v80, v100, time_);
  NAD_radical: olsen_2003__NAD_radical(NAD_radical0, v90, v50, v80, v100, v140, time_);
  NAD2: olsen_2003__NAD2(NAD20, v90, time_);
  H2O2: olsen_2003__H2O2(H2O20, v15, v20, v70, time_);
  Per3: olsen_2003__Per3(Per30, v20, v40, v60, v100, time_);
  Per2: olsen_2003__Per2(Per20, v100, v110, time_);
  coI: olsen_2003__coI(coI0, v20, v30, v80, time_);
  coII: olsen_2003__coII(coII0, v30, v40, time_);
  coIII: olsen_2003__coIII(coIII0, v60, v80, v110, time_);
  Ar_radical: olsen_2003__Ar_radical(Ar_radical0, v30, v40, v140, time_);
  ArH: olsen_2003__ArH(ArH0, v30, v40, v140, time_);
  v1: olsen_2003__v1(v15, NADH0, O20);
  v2: olsen_2003__v2(v20, H2O20, Per30);
  v3: olsen_2003__v3(v30, coI0, ArH0);
  v4: olsen_2003__v4(v40, coII0, ArH0);
  v5: olsen_2003__v5(v50, NAD_radical0, O20);
  v6: olsen_2003__v6(v60, O2_radical0, Per30);
  v7: olsen_2003__v7(v70, O2_radical0);
  v8: olsen_2003__v8(v80, coIII0, NAD_radical0);
  v9: olsen_2003__v9(v90, NAD_radical0);
  v10: olsen_2003__v10(v100, NAD_radical0, Per30);
  v11: olsen_2003__v11(v110, Per20, O20);
  v12: olsen_2003__v12(v120);
  v13: olsen_2003__v13(v130);
  v13_back: olsen_2003__v13_back(v13_back0, O20);
  v14: olsen_2003__v14(v140, Ar_radical0, NADH0);
end
