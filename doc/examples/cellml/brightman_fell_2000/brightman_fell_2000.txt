//Created by libAntimony v1.3
model brightman_fell_2000__environment(time_)
end

model brightman_fell_2000__Rs(Rs, v1, v3, time_)

  // Rate Rules:
  Rs' = v3 - v1;

  // Variable initializations:
  Rs = 11100;
end

model brightman_fell_2000__RL(RL, v1, v2, v4, time_)

  // Rate Rules:
  RL' = v1 - v2 - v4;

  // Variable initializations:
  RL = 0;
end

model brightman_fell_2000__Ri(Ri, v2, v3, v6, time_)

  // Rate Rules:
  Ri' = v2 + v3 + v6;

  // Variable initializations:
  Ri = 3900;
end

model brightman_fell_2000__L(L, v1, time_)

  // Rate Rules:
  L' = -(v1);

  // Variable initializations:
  L = 0.0000001;
end

model brightman_fell_2000__R2L2(R2L2, v4, v5, v7, time_)

  // Rate Rules:
  R2L2' = v4 - v5 - v7;

  // Variable initializations:
  R2L2 = 0;
end

model brightman_fell_2000__R2_CPP(R2_CPP, v7, v8, time_)

  // Rate Rules:
  R2_CPP' = v7 - v8;

  // Variable initializations:
  R2_CPP = 0;
end

model brightman_fell_2000__Li(Li, v2, v6, time_)

  // Rate Rules:
  Li' = v2 + v6;

  // Variable initializations:
  Li = 0;
end

model brightman_fell_2000__R2i(R2i, v5, v6, v8, time_)

  // Rate Rules:
  R2i' = v5 + v8 - v6;

  // Variable initializations:
  R2i = 0;
end

model brightman_fell_2000__Shc(Shc, v9, v10, time_)

  // Rate Rules:
  Shc' = v10 - v9;

  // Variable initializations:
  Shc = 30000;
end

model brightman_fell_2000__ShcP(ShcP, v10, v11, v27, time_)

  // Rate Rules:
  ShcP' = v27 - v10 - v11;

  // Variable initializations:
  ShcP = 0;
end

model brightman_fell_2000__ShcGS(ShcGS, v13, v11, v27, v12, time_)

  // Rate Rules:
  ShcGS' = v13 + v11 - v27 - v12;

  // Variable initializations:
  ShcGS = 0;
end

model brightman_fell_2000__GS(GS, v28, v11, time_)

  // Rate Rules:
  GS' = v28 - v11;

  // Variable initializations:
  GS = 20000;
end

model brightman_fell_2000__GSP(GSP, v27, v28, time_)

  // Rate Rules:
  GSP' = v27 - v28;

  // Variable initializations:
  GSP = 0;
end

model brightman_fell_2000__RasGDP(RasGDP, v15, v12, time_)

  // Rate Rules:
  RasGDP' = v15 - v12;

  // Variable initializations:
  RasGDP = 19800;
end

model brightman_fell_2000__Ras_ShcGS(Ras_ShcGS, v12, v13, time_)

  // Rate Rules:
  Ras_ShcGS' = v12 - v13;

  // Variable initializations:
  Ras_ShcGS = 0;
end

model brightman_fell_2000__RasGTP(RasGTP, RasGDP, v13, v17, v14, v16, time_)

  // Rate Rules:
  RasGTP' = v13 + v17 - v14 - v16;

  // Variable initializations:
  RasGTP = 200;
end

model brightman_fell_2000__GAP(GAP, v15, v14, time_)

  // Rate Rules:
  GAP' = v15 - v14;

  // Variable initializations:
  GAP = 15000;
end

model brightman_fell_2000__Ras_GAP(Ras_GAP, v14, v15, time_)

  // Rate Rules:
  Ras_GAP' = v14 - v15;

  // Variable initializations:
  Ras_GAP = 0;
end

model brightman_fell_2000__Raf(Raf, v18, v16, time_)

  // Rate Rules:
  Raf' = v18 - v16;

  // Variable initializations:
  Raf = 10000;
end

model brightman_fell_2000__Ras_Raf(Ras_Raf, v16, v17, time_)

  // Rate Rules:
  Ras_Raf' = v16 - v17;

  // Variable initializations:
  Ras_Raf = 0;
end

model brightman_fell_2000__Rafa(Rafa, v17, v18, v19, v21, time_)

  // Rate Rules:
  Rafa' = v17 - v19 - v21 - v18;

  // Variable initializations:
  Rafa = 0;
end

model brightman_fell_2000__MEK(MEK, v20, v19, time_)

  // Rate Rules:
  MEK' = v20 - v19;

  // Variable initializations:
  MEK = 360000;
end

model brightman_fell_2000__MEKP(MEKP, v19, v20, v21, v22, time_)

  // Rate Rules:
  MEKP' = v19 + v22 - v20 - v21;

  // Variable initializations:
  MEKP = 0;
end

model brightman_fell_2000__MEKPP(MEKPP, v21, v22, time_)

  // Rate Rules:
  MEKPP' = v21 - v22;

  // Variable initializations:
  MEKPP = 0;
end

model brightman_fell_2000__ERK(ERK, v24, v23, time_)

  // Rate Rules:
  ERK' = v24 - v23;

  // Variable initializations:
  ERK = 750000;
end

model brightman_fell_2000__ERKP(ERKP, v23, v26, v24, v25, time_)

  // Rate Rules:
  ERKP' = v23 + v26 - v24 - v25;

  // Variable initializations:
  ERKP = 0;
end

model brightman_fell_2000__ERKPP(ERKPP, v25, v26, time_)

  // Rate Rules:
  ERKPP' = v25 - v26;

  // Variable initializations:
  ERKPP = 0;
end

model brightman_fell_2000__t(t, v29, time_)

  // Rate Rules:
  t' = v29;

  // Variable initializations:
  t = 0;
end

model brightman_fell_2000__X(X, v30, time_)

  // Rate Rules:
  X' = v30;

  // Variable initializations:
  X = 0;
end

model brightman_fell_2000__v1(v1, Rs, L, RL)

  // Assignment Rules:
  v1 := k1 * Rs * L - kn1 * RL;

  // Variable initializations:
  k1 = 384210000;
  kn1 = 0.73;
end

model brightman_fell_2000__v2(v2, RL, t)

  // Assignment Rules:
  v2 := f * k2 * (E + (1 - E) * (1 - exp(-(power(t / DT, 3 ))))) * RL;

  // Variable initializations:
  DT = 6.5;
  E = 0.12;
  k2 = 0.7;
  f = 0.2;
end

model brightman_fell_2000__v3(v3, Rs, Ri, t)

  // Assignment Rules:
  v3 := k3 * Ri - f * kn3 * (E + (1 - E) * (1 - exp(-(power(t / DT, 3 ))))) * Rs;

  // Variable initializations:
  DT = 6.5;
  E = 0.12;
  kn3 = 0.7;
  f = 0.2;
  k3 = 0.0484;
end

model brightman_fell_2000__v4(v4, RL, R2L2)

  // Assignment Rules:
  v4 := k4 * RL * RL - k2_4 * R2L2;

  // Variable initializations:
  k2_4 = 0.0000001;
  k4 = 0.001383;
end

model brightman_fell_2000__v5(v5, R2L2, t)

  // Assignment Rules:
  v5 := f * k5 * (E + (1 - E) * (1 - exp(-(power(t / DT, 3 ))))) * R2L2;

  // Variable initializations:
  DT = 6.5;
  E = 0.12;
  k5 = 0.35;
  f = 0.2;
end

model brightman_fell_2000__v6(v6, R2i, t)

  // Assignment Rules:
  v6 := k6 * (E + (1 - E) * (1 - exp(-(power(t / DT, 3 ))))) * R2i;

  // Variable initializations:
  DT = 6.5;
  E = 0.12;
  k6 = 0.35;
end

model brightman_fell_2000__v7(v7, R2L2, R2_CPP)

  // Assignment Rules:
  v7 := k7 * f * R2L2 - kn7 * R2_CPP;

  // Variable initializations:
  k7 = 1;
  f = 0.2;
  kn7 = 0.000347;
end

model brightman_fell_2000__v8(v8, R2_CPP, t)

  // Assignment Rules:
  v8 := k8 * (E + (1 - E) * (1 - exp(-(power(t / DT, 3 ))))) * R2_CPP;

  // Variable initializations:
  DT = 6.5;
  E = 0.12;
  k8 = 0.35;
end

model brightman_fell_2000__v9(v9, R2L2, R2i, R2_CPP, Shc)

  // Assignment Rules:
  v9 := k9 * 2 * (R2L2 + R2i + R2_CPP) * Shc / (K_9 + Shc);

  // Variable initializations:
  k9 = 12;
  K_9 = 6000;
end

model brightman_fell_2000__v10(v10, ShcP)

  // Assignment Rules:
  v10 := V_10 * ShcP / (K_10 + ShcP);

  // Variable initializations:
  V_10 = 300000;
  K_10 = 6000;
end

model brightman_fell_2000__v11(v11, ShcP, GS, ShcGS)

  // Assignment Rules:
  v11 := k11 * ShcP * GS - kn11 * ShcGS;

  // Variable initializations:
  k11 = 0.002;
  kn11 = 3.81;
end

model brightman_fell_2000__v12(v12, RasGDP, ShcGS, Ras_ShcGS)

  // Assignment Rules:
  v12 := k12 * RasGDP * ShcGS - kn12 * Ras_ShcGS;

  // Variable initializations:
  k12 = 0.0163;
  kn12 = 10;
end

model brightman_fell_2000__v13(v13, Ras_ShcGS)

  // Assignment Rules:
  v13 := k_13 * Ras_ShcGS;

  // Variable initializations:
  k_13 = 15;
end

model brightman_fell_2000__v14(v14, RasGTP, GAP, Ras_GAP)

  // Assignment Rules:
  v14 := k14 * RasGTP * GAP - kn14 * Ras_GAP;

  // Variable initializations:
  k14 = 0.005;
  kn14 = 60;
end

model brightman_fell_2000__v15(v15, Ras_GAP)

  // Assignment Rules:
  v15 := k15 * Ras_GAP;

  // Variable initializations:
  k15 = 720;
end

model brightman_fell_2000__v16(v16, RasGTP, Raf, Ras_Raf)

  // Assignment Rules:
  v16 := k16 * RasGTP * Raf - kn16 * Ras_Raf;

  // Variable initializations:
  k16 = 0.0012;
  kn16 = 3;
end

model brightman_fell_2000__v17(v17, Ras_Raf)

  // Assignment Rules:
  v17 := k17 * Ras_Raf;

  // Variable initializations:
  k17 = 27;
end

model brightman_fell_2000__v18(v18, Rafa)

  // Assignment Rules:
  v18 := V_18 * Rafa / (K_18 + Rafa);

  // Variable initializations:
  V_18 = 97000;
  K_18 = 6000;
end

model brightman_fell_2000__v19(v19, Rafa, MEK)

  // Assignment Rules:
  v19 := Rafa * MEK * k19 / (K_19 + MEK);

  // Variable initializations:
  k19 = 50;
  K_19 = 9000;
end

model brightman_fell_2000__v20(v20, MEKP)

  // Assignment Rules:
  v20 := V_20 * MEKP / (K_20 + MEKP);

  // Variable initializations:
  V_20 = 920000;
  K_20 = 600000;
end

model brightman_fell_2000__v21(v21, Rafa, MEKP)

  // Assignment Rules:
  v21 := Rafa * MEKP * k21 / (K_21 + MEKP);

  // Variable initializations:
  k21 = 50;
  K_21 = 9000;
end

model brightman_fell_2000__v22(v22, MEKPP)

  // Assignment Rules:
  v22 := V_22 * MEKPP / (K_22 + MEKPP);

  // Variable initializations:
  V_22 = 920000;
  K_22 = 600000;
end

model brightman_fell_2000__v23(v23, ERK, MEKP, MEKPP)

  // Assignment Rules:
  v23 := k23 * ERK * (MEKP + MEKPP) / (K_23 + ERK);

  // Variable initializations:
  k23 = 8.3;
  K_23 = 90000;
end

model brightman_fell_2000__v24(v24, ERKP)

  // Assignment Rules:
  v24 := V_24 * ERKP / (K_24 + ERKP);

  // Variable initializations:
  V_24 = 200000;
  K_24 = 600000;
end

model brightman_fell_2000__v25(v25, ERKP, MEKP, MEKPP)

  // Assignment Rules:
  v25 := k25 * ERKP * (MEKP + MEKPP) / (K_25 + ERKP);

  // Variable initializations:
  k25 = 8.3;
  K_25 = 90000;
end

model brightman_fell_2000__v26(v26, ERKPP)

  // Assignment Rules:
  v26 := V_26 * ERKPP / (K_26 + ERKPP);

  // Variable initializations:
  V_26 = 400000;
  K_26 = 600000;
end

model brightman_fell_2000__v27(v27, ERKPP, ShcGS)

  // Assignment Rules:
  v27 := ERKPP * ShcGS * k27 / (K_27 + ShcGS);

  // Variable initializations:
  k27 = 1.6;
  K_27 = 600000;
end

model brightman_fell_2000__v28(v28, GSP)

  // Assignment Rules:
  v28 := V_28 * GSP / (K_28 + GSP);

  // Variable initializations:
  V_28 = 75;
  K_28 = 20000;
end

model brightman_fell_2000__v29(v29)

  // Assignment Rules:
  v29 := v_1;

  // Variable initializations:
  v_1 = 1;
end

model brightman_fell_2000__v30(v30, t)

  // Assignment Rules:
  v30 := k_11 * t;

  // Variable initializations:
  k_11 = 0;
end

model brightman_fell_2000____main()

  // Sub-modules, and any changes to those submodules:
  environment: brightman_fell_2000__environment(time_);
  Rs: brightman_fell_2000__Rs(Rs0, v110, v31, time_);
  RL: brightman_fell_2000__RL(RL0, v110, v210, v40, time_);
  Ri: brightman_fell_2000__Ri(Ri0, v210, v31, v60, time_);
  L: brightman_fell_2000__L(L0, v110, time_);
  R2L2: brightman_fell_2000__R2L2(R2L20, v40, v50, v70, time_);
  R2_CPP: brightman_fell_2000__R2_CPP(R2_CPP0, v70, v80, time_);
  Li: brightman_fell_2000__Li(Li0, v210, v60, time_);
  R2i: brightman_fell_2000__R2i(R2i0, v50, v60, v80, time_);
  Shc: brightman_fell_2000__Shc(Shc0, v90, v100, time_);
  ShcP: brightman_fell_2000__ShcP(ShcP0, v100, v111, v270, time_);
  ShcGS: brightman_fell_2000__ShcGS(ShcGS0, v130, v111, v270, v120, time_);
  GS: brightman_fell_2000__GS(GS0, v280, v111, time_);
  GSP: brightman_fell_2000__GSP(GSP0, v270, v280, time_);
  RasGDP: brightman_fell_2000__RasGDP(RasGDP0, v150, v120, time_);
  Ras_ShcGS: brightman_fell_2000__Ras_ShcGS(Ras_ShcGS0, v120, v130, time_);
  RasGTP: brightman_fell_2000__RasGTP(RasGTP0, RasGDP0, v130, v170, v140, v160, time_);
  GAP: brightman_fell_2000__GAP(GAP0, v150, v140, time_);
  Ras_GAP: brightman_fell_2000__Ras_GAP(Ras_GAP0, v140, v150, time_);
  Raf: brightman_fell_2000__Raf(Raf0, v180, v160, time_);
  Ras_Raf: brightman_fell_2000__Ras_Raf(Ras_Raf0, v160, v170, time_);
  Rafa: brightman_fell_2000__Rafa(Rafa0, v170, v180, v190, v211, time_);
  MEK: brightman_fell_2000__MEK(MEK0, v200, v190, time_);
  MEKP: brightman_fell_2000__MEKP(MEKP0, v190, v200, v211, v220, time_);
  MEKPP: brightman_fell_2000__MEKPP(MEKPP0, v211, v220, time_);
  ERK: brightman_fell_2000__ERK(ERK0, v240, v230, time_);
  ERKP: brightman_fell_2000__ERKP(ERKP0, v230, v260, v240, v250, time_);
  ERKPP: brightman_fell_2000__ERKPP(ERKPP0, v250, v260, time_);
  t: brightman_fell_2000__t(t0, v290, time_);
  X: brightman_fell_2000__X(X0, v300, time_);
  v1: brightman_fell_2000__v1(v110, Rs0, L0, RL0);
  v2: brightman_fell_2000__v2(v210, RL0, t0);
  v3: brightman_fell_2000__v3(v31, Rs0, Ri0, t0);
  v4: brightman_fell_2000__v4(v40, RL0, R2L20);
  v5: brightman_fell_2000__v5(v50, R2L20, t0);
  v6: brightman_fell_2000__v6(v60, R2i0, t0);
  v7: brightman_fell_2000__v7(v70, R2L20, R2_CPP0);
  v8: brightman_fell_2000__v8(v80, R2_CPP0, t0);
  v9: brightman_fell_2000__v9(v90, R2L20, R2i0, R2_CPP0, Shc0);
  v10: brightman_fell_2000__v10(v100, ShcP0);
  v11: brightman_fell_2000__v11(v111, ShcP0, GS0, ShcGS0);
  v12: brightman_fell_2000__v12(v120, RasGDP0, ShcGS0, Ras_ShcGS0);
  v13: brightman_fell_2000__v13(v130, Ras_ShcGS0);
  v14: brightman_fell_2000__v14(v140, RasGTP0, GAP0, Ras_GAP0);
  v15: brightman_fell_2000__v15(v150, Ras_GAP0);
  v16: brightman_fell_2000__v16(v160, RasGTP0, Raf0, Ras_Raf0);
  v17: brightman_fell_2000__v17(v170, Ras_Raf0);
  v18: brightman_fell_2000__v18(v180, Rafa0);
  v19: brightman_fell_2000__v19(v190, Rafa0, MEK0);
  v20: brightman_fell_2000__v20(v200, MEKP0);
  v21: brightman_fell_2000__v21(v211, Rafa0, MEKP0);
  v22: brightman_fell_2000__v22(v220, MEKPP0);
  v23: brightman_fell_2000__v23(v230, ERK0, MEKP0, MEKPP0);
  v24: brightman_fell_2000__v24(v240, ERKP0);
  v25: brightman_fell_2000__v25(v250, ERKP0, MEKP0, MEKPP0);
  v26: brightman_fell_2000__v26(v260, ERKPP0);
  v27: brightman_fell_2000__v27(v270, ERKPP0, ShcGS0);
  v28: brightman_fell_2000__v28(v280, GSP0);
  v29: brightman_fell_2000__v29(v290);
  v30: brightman_fell_2000__v30(v300, t0);
end
