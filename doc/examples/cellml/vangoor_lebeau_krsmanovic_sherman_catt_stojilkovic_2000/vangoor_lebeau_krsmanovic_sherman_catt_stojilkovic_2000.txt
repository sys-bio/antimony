//Created by libAntimony v1.3
model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__environment(time_)
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__membrane(V, time_, i_Na, i_Ca_L, i_Ca_T, i_K_dr, i_M, i_ir, i_d)

  // Rate Rules:
  V' = ( i_app - ( i_Na + i_Ca_L + i_Ca_T + i_K_dr + i_M + i_ir + i_d )) / Cm;

  // Variable initializations:
  Cm = 14.0;
  i_app = 15.0;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__sodium_current(i_Na, time_, V, A)

  // Assignment Rules:
  i_Na := g_Na * O * ( V - V_Na );
  O := power( A , 3.0 );

  // Variable initializations:
  g_Na = 60.0;
  V_Na = 60.0;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__A(A, k1, k1_, alpha, beta, D, A_, time_)

  // Rate Rules:
  A' = alpha * D + k1_ * A_ - ( beta * A + k1 * A );
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__A_(A_, k1, k1_, alpha, beta, a, D_, A, time_)

  // Rate Rules:
  A_' = alpha * a * D_ + k1 * A - ( beta / a * A_ + k1_ * A_ );
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__D(D, k2, k2_, alpha, beta, A, D_, time_)

  // Rate Rules:
  D' = beta * A + k2_ * D_ - ( alpha * D + k2 * D );
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__D_(D_, k2, k2_, alpha, beta, a, D, A_, time_)

  // Rate Rules:
  D_' = beta / a * A_ + k2 * D - ( alpha * a * D_ + k2_ * D_ );
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__reaction_constants(k1, k1_, k2, k2_, alpha, beta, a, V)

  // Assignment Rules:
  alpha := 10.0 / ( 1.0 + exp(-(( V + 6.0 )) / 10.0 ));
  beta := 10.0 / ( 1.0 + exp(( V + 54.4 ) / 4.6 ));
  a := power( k1 * k2_ / ( k1_ * k2 ), 0.5 );

  // Variable initializations:
  k1 = 0.3;
  k1_ = 0.03;
  k2 = 0.001;
  k2_ = 0.01;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__L_type_calcium_current_m_gate(m, V, time_)

  // Assignment Rules:
  m_infinity := 1.0 / ( 1.0 + exp(-(( V - Vh )) / 12.0 ));
  tau_m := 5.0 / (exp(( V + 15.0 ) / 25.0 ) + exp(-(( V + 15.0 )) / 25.0 ));

  // Rate Rules:
  m' = ( m_infinity - m ) / tau_m;

  // Variable initializations:
  Vh = 40.0;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__L_type_calcium_current(i_Ca_L, V_Ca, time_, V, m)

  // Sub-modules, and any changes to those submodules:
  L_type_calcium_current_m_gate: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__L_type_calcium_current_m_gate(m, V, time_);

  // Assignment Rules:
  i_Ca_L := g_Ca_L * power( m , 2.0 ) * ( V - V_Ca );

  // Variable initializations:
  V_Ca = 100.0;
  g_Ca_L = 1.3;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__T_type_calcium_current_m_gate(m, V, time_)

  // Assignment Rules:
  m_infinity := 1.0 / ( 1.0 + exp(-(( V - 56.1 )) / 10.0 ));
  tau_m := 7.0 / (exp(( V + 50.0 ) / 9.0 ) + exp(-(( V + 50.0 )) / 9.0 )) + 0.8;

  // Rate Rules:
  m' = ( m_infinity - m ) / tau_m;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__T_type_calcium_current_h_gate(h, V, time_)

  // Assignment Rules:
  h_infinity := 1.0 / ( 1.0 + exp(( V + 86.4 ) / 4.7 ));

  // Rate Rules:
  h' = ( h_infinity - h ) / tau_h;

  // Variable initializations:
  tau_h = 22.0;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__T_type_calcium_current(i_Ca_T, time_, V, V_Ca, m, h)

  // Sub-modules, and any changes to those submodules:
  T_type_calcium_current_m_gate: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__T_type_calcium_current_m_gate(m, V, time_);
  T_type_calcium_current_h_gate: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__T_type_calcium_current_h_gate(h, V, time_);

  // Assignment Rules:
  i_Ca_T := g_Ca_T * power( m , 2.0 ) * h * ( V - V_Ca );

  // Variable initializations:
  g_Ca_T = 0.94;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__delayed_rectifier_K_channel_current_n_gate(n, V, time_)

  // Assignment Rules:
  n_infinity := 1.0 / ( 1.0 + exp(-(( V + 25.0 )) / 15.0 ));
  tau_n := 15.0 / (exp(( V + 30.0 ) / 15.0 ) + exp(-(( V + 30.0 )) / 15.0 )) + 1.0;

  // Rate Rules:
  n' = ( n_infinity - n ) / tau_n;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__delayed_rectifier_K_channel_current_h_gate(h, V, time_)

  // Assignment Rules:
  h_infinity := 0.7 / ( 1.0 + exp(-(( V + 35.0 )) / 10.0 )) + 0.3;

  // Rate Rules:
  h' = ( h_infinity - h ) / tau_h;

  // Variable initializations:
  tau_h = 1000.0;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__delayed_rectifier_K_channel_current(i_K_dr, V_K, time_, V, n, h)

  // Sub-modules, and any changes to those submodules:
  delayed_rectifier_K_channel_current_n_gate: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__delayed_rectifier_K_channel_current_n_gate(n, V, time_);
  delayed_rectifier_K_channel_current_h_gate: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__delayed_rectifier_K_channel_current_h_gate(h, V, time_);

  // Assignment Rules:
  i_K_dr := g_K_dr * power( n , 4.0 ) * h * ( V - V_K );

  // Variable initializations:
  V_K = -80.0;
  g_K_dr = 20.0;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__M_like_K_current_n_gate(n, V, time_)

  // Assignment Rules:
  n_infinity := 1.0 / ( 1.0 + exp(-(( V + 37.0 )) / 4.0 ));
  tau_n := 80.0 / (exp(( V + 30.0 ) / 15.0 ) + exp(-(( V + 30.0 )) / 15.0 ));

  // Rate Rules:
  n' = ( n_infinity - n ) / tau_n;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__M_like_K_current(i_M, time_, V, V_K, n)

  // Sub-modules, and any changes to those submodules:
  M_like_K_current_n_gate: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__M_like_K_current_n_gate(n, V, time_);

  // Assignment Rules:
  i_M := g_M * n * ( V - V_K );

  // Variable initializations:
  g_M = 4.0;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__inward_rectifier_K_current_n_gate(n_infinity, V)

  // Assignment Rules:
  n_infinity := 0.8 * ( 1.0 / ( 1.0 + exp(( V + 80.0 ) / 12.0 ))) + 0.2;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__inward_rectifier_K_current(i_ir, time_, V, V_K, n_infinity)

  // Sub-modules, and any changes to those submodules:
  inward_rectifier_K_current_n_gate: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__inward_rectifier_K_current_n_gate(n_infinity, V);

  // Variable initializations:
  g_ir = 1.71;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__inward_leak_current(i_d, time_, V, V_Ca)

  // Assignment Rules:
  i_d := g_d * ( V - V_Ca );

  // Variable initializations:
  g_d = 0.044;
end

model vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01____main()

  // Sub-modules, and any changes to those submodules:
  environment: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__environment(time_);
  membrane: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__membrane(V, time_, i_Na, i_Ca_L, i_Ca_T, i_K_dr, i_M, i_ir, i_d);
  sodium_current: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__sodium_current(i_Na, time_, V, A0);
  A: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__A(A0, k1, k1_, alpha, beta, D0, A_0, time_);
  A_: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__A_(A_0, k1, k1_, alpha, beta, a, D_0, A0, time_);
  D: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__D(D0, k2, k2_, alpha, beta, A0, D_0, time_);
  D_: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__D_(D_0, k2, k2_, alpha, beta, a, D0, A_0, time_);
  reaction_constants: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__reaction_constants(k1, k1_, k2, k2_, alpha, beta, a, V);
  L_type_calcium_current: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__L_type_calcium_current(i_Ca_L, V_Ca, time_, V, m);
  T_type_calcium_current: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__T_type_calcium_current(i_Ca_T, time_, V, V_Ca, m0, h);
  delayed_rectifier_K_channel_current: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__delayed_rectifier_K_channel_current(i_K_dr, V_K, time_, V, n, h0);
  M_like_K_current: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__M_like_K_current(i_M, time_, V, V_K, n0);
  inward_rectifier_K_current: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__inward_rectifier_K_current(i_ir, time_, V, V_K, n_infinity);
  inward_leak_current: vangoor_lebeau_krsmanovic_sherman_catt_stojilkovic_2000_version01__inward_leak_current(i_d, time_, V, V_Ca);
end
