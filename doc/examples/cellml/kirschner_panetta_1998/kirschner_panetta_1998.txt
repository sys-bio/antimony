//Created by libAntimony v1.3

//Warnings from automatic translation:
//    The units of "tau" ('dimensionless') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "tau" ('dimensionless') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "tau" ('dimensionless') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.

model kirschner_panetta_1998_version01__environment(tau)
end

model kirschner_panetta_1998_version01__x(tau, y, x, z)

  // Variable initializations:
  mu2 = 0.03;
  c = 0.025;
  p1 = 0.1245;
  g1 = 2.0E-7;
  s1 = 0;
end

model kirschner_panetta_1998_version01__y(y, x, tau)

  // Variable initializations:
  y = 1;
  r2 = 0.18;
  a = 1.0;
  b = 1.0E-9;
  g2 = 1.0E5;
end

model kirschner_panetta_1998_version01__z(z, x, y, tau)

  // Variable initializations:
  mu3 = 10.0;
  p2 = 5.0;
  g3 = 1.0E3;
  s2 = 0;
end

model kirschner_panetta_1998_version01____main()

  // Sub-modules, and any changes to those submodules:
  environment: kirschner_panetta_1998_version01__environment(tau);
  x: kirschner_panetta_1998_version01__x(tau, y0, x0, z0);
  y: kirschner_panetta_1998_version01__y(y0, x0, tau);
  z: kirschner_panetta_1998_version01__z(z0, x0, y0, tau);
end
