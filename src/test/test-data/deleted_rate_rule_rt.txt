// Created by libAntimony v3.2.0
model sub(x)

  // Rate Rules:
  x' = 1;

  // Variable initializations:
  x = 3;

  // Other declarations:
  var x;
end

model *deleted_rate_rule()

  // Sub-modules, and any changes to those submodules:
  A: sub(x);

  // Rate Rules:
  x' = ;

  // Variable initializations:
  x = 5;
end
