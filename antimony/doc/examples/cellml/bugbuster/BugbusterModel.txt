//Created by libAntimony v1.3
model BugbusterModel_SVBM__Subtilin(concentration)

  // Variable initializations:
  concentration = 1000;
end

model BugbusterModel_SVBM__SpeciesInitialConcentrations(RNA1, RNA2, spaK, spaR, spaKactive, spaRactive, GFP)

  // Variable initializations:
  RNA1 = 0;
  RNA2 = 0;
  spaK = 0;
  spaR = 0;
  spaKactive = 0;
  spaRactive = 0;
  GFP = 0;
end

model BugbusterModel_SVBM__RNA1_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model BugbusterModel_SVBM__spaK_interface(JPlusProduction, JPlusspaRActivation, JPlusspaKActiveDephosphorylation, JMinusDegradation, JMinusspaKActivation, JGain)

  // Assignment Rules:
  JGain := JPlusProduction + JPlusspaRActivation + JPlusspaKActiveDephosphorylation - (JMinusspaKActivation + JMinusDegradation);
end

model BugbusterModel_SVBM__spaKactive_interface(JPlusspaKActivation, JMinusDegradation, JMinusDephosphorylation, JMinusspaRActivation, JGain)

  // Assignment Rules:
  JGain := JPlusspaKActivation - (JMinusDegradation + JMinusDephosphorylation + JMinusspaRActivation);
end

model BugbusterModel_SVBM__spaR_interface(JPlusProduction, JMinusDegradation, JMinusspaRActivation, JPlusspaRActiveDephosphorylation, JGain)

  // Assignment Rules:
  JGain := JPlusProduction + JPlusspaRActiveDephosphorylation - (JMinusDegradation + JMinusspaRActivation);
end

model BugbusterModel_SVBM__spaRactive_interface(JMinusDegradation, JMinusDephosphorylation, JPlusspaRActivation, JGain)

  // Assignment Rules:
  JGain := JPlusspaRActivation - (JMinusDegradation + JMinusDephosphorylation);
end

model BugbusterModel_SVBM__RNA2_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model BugbusterModel_SVBM__GFP_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model Template_Chassis_WellStirredBag__WellStirredBag(bagVolume)
end

model Chassis_Bacillus__Chassis_Bacillus(cellVolume)

  // Sub-modules, and any changes to those submodules:
  WellStirredBag: Template_Chassis_WellStirredBag__WellStirredBag(cellVolume);

  // Variable initializations:
  cellVolume = 1;
end

model Time__Time(time_)
end

model Template_Promoter_Constitutive__Template_Promoter_Constitutive(JRNA, localVolume, k)

  // Assignment Rules:
  JRNA := k * 1e9 / (localVolume * 1e-15 * avogadrosConstant);

  // Variable initializations:
  avogadrosConstant = 6.0221415e23;
end

model Bugbuster_Promoter1__Bugbuster_Promoter1(localVolume, k, JRNA)

  // Sub-modules, and any changes to those submodules:
  Template_Promoter_Constitutive: Template_Promoter_Constitutive__Template_Promoter_Constitutive(JRNA, localVolume, k);

  // Variable initializations:
  k = 0.04;
end

model Template_Species__Template_Species(time_, concentration, concentrationInitialValue, JGain)

  // Rate Rules:
  concentration' = JGain;

  // Variable initializations:
  concentration = concentrationInitialValue;
end

model Template_RxR1P1__Template_RxR1P1(concentration, k, J)

  // Assignment Rules:
  J := k * concentration;
end

model Bioenvironment_Degradation_BugbusterRNA1__Bioenvironment_Degradation_BugbusterRNA1(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 0.0058;
end

model Template_RBS__Template_RBS(RNA, RNARiPs, k, localVolume)

  // Assignment Rules:
  RNARiPs := 1e9 * k * RNA * localVolume * 1e-15;
end

model Bugbuster_RBS_spaR__Bugbuster_RBS_spaR(localVolume, RNA, RNARiPs, k)

  // Sub-modules, and any changes to those submodules:
  Template_RBS: Template_RBS__Template_RBS(RNA, RNARiPs, k, localVolume);

  // Variable initializations:
  k = 0.1;
end

model Template_ProteinCDS__Template_ProteinCDS(localVolume, hostRNARiPs, J)

  // Assignment Rules:
  J := 1e6 * hostRNARiPs / localVolume;
end

model Bugbuster_ProteinCDS_spaR__Bugbuster_ProteinCDS_spaR(localVolume, hostRNARiPs, J)

  // Sub-modules, and any changes to those submodules:
  Template_ProteinCDS: Template_ProteinCDS__Template_ProteinCDS(localVolume, hostRNARiPs, J);
end

model Bugbuster_RBS_spaK__Bugbuster_RBS_spaK(localVolume, RNA, RNARiPs, k)

  // Sub-modules, and any changes to those submodules:
  Template_RBS: Template_RBS__Template_RBS(RNA, RNARiPs, k, localVolume);

  // Variable initializations:
  k = 0.1818;
end

model Bugbuster_ProteinCDS_spaK__Bugbuster_ProteinCDS_spaK(localVolume, hostRNARiPs, J)

  // Sub-modules, and any changes to those submodules:
  Template_ProteinCDS: Template_ProteinCDS__Template_ProteinCDS(localVolume, hostRNARiPs, J);
end

model Bioenvironment_Degradation_spaK__Bioenvironment_Degradation_spaK(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 0.0012;
end

model Template_RxR2P1__Template_RxR2P1(J, reactant1, reactant2, k)

  // Assignment Rules:
  J := reactant1 * reactant2 * k;
end

model Bioenvironment_RxR2P1_spaKActivation__Bioenvironment_RxR2P1_spaKActivation(J, reactant1, reactant2, k)

  // Sub-modules, and any changes to those submodules:
  Template_RxR2P1: Template_RxR2P1__Template_RxR2P1(J, reactant1, reactant2, k);

  // Variable initializations:
  k = 1e-4;
end

model Bioenvironment_Dephosphorylation_spaKactive__Bioenvironment_Dephosphorylation_spaKactive(concentration, k, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, k, J);

  // Variable initializations:
  k = 0.1;
end

model Bioenvironment_Degradation_spaKactive__Bioenvironment_Degradation_spaKactive(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 0.0012;
end

model Bioenvironment_Degradation_spaR__Bioenvironment_Degradation_spaR(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 0.0012;
end

model Bioenvironment_RxR2P1_spaRActivation__Bioenvironment_RxR2P1_spaRActivation(J, reactant1, reactant2, k)

  // Sub-modules, and any changes to those submodules:
  Template_RxR2P1: Template_RxR2P1__Template_RxR2P1(J, reactant1, reactant2, k);

  // Variable initializations:
  k = 1e-4;
end

model Bioenvironment_Dephosphorylation_spaRactive__Bioenvironment_Dephosphorylation_spaRactive(concentration, k, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, k, J);

  // Variable initializations:
  k = 0.1;
end

model Bioenvironment_Degradation_spaRactive__Bioenvironment_Degradation_spaRactive(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 0.0012;
end

model Template_Promoter_Inductive__Template_Promoter_Inductive(JRNA, localVolume, k, tFactor, n, Km)

  // Assignment Rules:
  JRNA := k * power(tFactor, n) / (power(Km, n) + power(tFactor, n)) * 1e9 / (localVolume * 1e-15 * avogadrosConstant);

  // Variable initializations:
  avogadrosConstant = 6.0221415e23;
end

model Bugbuster_Promoter2__Bugbuster_Promoter2(JRNA, localVolume, k, tFactor, n, Km)

  // Sub-modules, and any changes to those submodules:
  Template_Promoter_Inductive: Template_Promoter_Inductive__Template_Promoter_Inductive(JRNA, localVolume, k, tFactor, n, Km);

  // Variable initializations:
  k = 0.04;
  n = 2;
  Km = 320;
end

model Bioenvironment_Degradation_BugbusterRNA2__Bioenvironment_Degradation_BugbusterRNA2(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 0.0058;
end

model Bugbuster_RBS_GFP__Bugbuster_RBS_GFP(localVolume, RNA, RNARiPs, k)

  // Sub-modules, and any changes to those submodules:
  Template_RBS: Template_RBS__Template_RBS(RNA, RNARiPs, k, localVolume);

  // Variable initializations:
  k = 0.145;
end

model Bugbuster_ProteinCDS_GFP__Bugbuster_ProteinCDS_GFP(localVolume, hostRNARiPs, J)

  // Sub-modules, and any changes to those submodules:
  Template_ProteinCDS: Template_ProteinCDS__Template_ProteinCDS(localVolume, hostRNARiPs, J);
end

model Bioenvironment_Degradation_GFP__Bioenvironment_Degradation_GFP(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 0.0012;
end

model BugbusterModel_SVBM____main()

  // Sub-modules, and any changes to those submodules:
  Subtilin: BugbusterModel_SVBM__Subtilin(concentration1);
  SpeciesInitialConcentrations: BugbusterModel_SVBM__SpeciesInitialConcentrations(RNA10, RNA20, spaK0, spaR0, spaKactive0, spaRactive0, GFP0);
  RNA1_interface: BugbusterModel_SVBM__RNA1_interface(JRNA, JGain, J);
  spaK_interface: BugbusterModel_SVBM__spaK_interface(J0, J4, J3, J1, J2, JGain0);
  spaKactive_interface: BugbusterModel_SVBM__spaKactive_interface(J2, J5, J3, J4, JGain1);
  spaR_interface: BugbusterModel_SVBM__spaR_interface(J6, J7, J4, J8, JGain2);
  spaRactive_interface: BugbusterModel_SVBM__spaRactive_interface(J9, J8, J4, JGain3);
  RNA2_interface: BugbusterModel_SVBM__RNA2_interface(JRNA0, JGain4, J10);
  GFP_interface: BugbusterModel_SVBM__GFP_interface(J11, JGain5, J12);
  ChassisBacillus: Chassis_Bacillus__Chassis_Bacillus(cellVolume);
  Time_: Time__Time(time_);
  Bugbuster_Promoter1: Bugbuster_Promoter1__Bugbuster_Promoter1(cellVolume, k, JRNA);
  RNA1: Template_Species__Template_Species(time_, concentration, RNA10, JGain);
  Bioenvironment_Degradation_RNA1: Bioenvironment_Degradation_BugbusterRNA1__Bioenvironment_Degradation_BugbusterRNA1(concentration, kDecay, J);
  Bugbuster_RBS_spaR: Bugbuster_RBS_spaR__Bugbuster_RBS_spaR(cellVolume, concentration, RNARiPs, k0);
  Bugbuster_ProteinCDS_spaR: Bugbuster_ProteinCDS_spaR__Bugbuster_ProteinCDS_spaR(cellVolume, RNARiPs, J6);
  Bugbuster_RBS_spaK: Bugbuster_RBS_spaK__Bugbuster_RBS_spaK(cellVolume, concentration, RNARiPs0, k1);
  Bugbuster_ProteinCDS_spaK: Bugbuster_ProteinCDS_spaK__Bugbuster_ProteinCDS_spaK(cellVolume, RNARiPs0, J0);
  spaK: Template_Species__Template_Species(time_, concentration0, spaK0, JGain0);
  Bioenvironment_Degradation_spaK: Bioenvironment_Degradation_spaK__Bioenvironment_Degradation_spaK(concentration0, kDecay0, J1);
  spaKActivation: Bioenvironment_RxR2P1_spaKActivation__Bioenvironment_RxR2P1_spaKActivation(J2, concentration1, concentration0, k2);
  spaKactive: Template_Species__Template_Species(time_, concentration2, spaKactive0, JGain1);
  Bioenvironment_Dephosphorylation_spaKactive: Bioenvironment_Dephosphorylation_spaKactive__Bioenvironment_Dephosphorylation_spaKactive(concentration2, k3, J3);
  Bioenvironment_Degradation_spaKactive: Bioenvironment_Degradation_spaKactive__Bioenvironment_Degradation_spaKactive(concentration2, kDecay1, J5);
  spaR: Template_Species__Template_Species(time_, concentration3, spaR0, JGain2);
  Bioenvironment_Degradation_spaR: Bioenvironment_Degradation_spaR__Bioenvironment_Degradation_spaR(concentration3, kDecay2, J7);
  spaRActivation: Bioenvironment_RxR2P1_spaRActivation__Bioenvironment_RxR2P1_spaRActivation(J4, concentration2, concentration3, k4);
  spaRactive: Template_Species__Template_Species(time_, concentration4, spaRactive0, JGain3);
  Bioenvironment_Dephosphorylation_spaRactive: Bioenvironment_Dephosphorylation_spaRactive__Bioenvironment_Dephosphorylation_spaRactive(concentration4, k5, J8);
  Bioenvironment_Degradation_spaRactive: Bioenvironment_Degradation_spaRactive__Bioenvironment_Degradation_spaRactive(concentration4, kDecay3, J9);
  Bugbuster_Promoter2: Bugbuster_Promoter2__Bugbuster_Promoter2(JRNA0, cellVolume, k6, concentration4, n, Km);
  RNA2: Template_Species__Template_Species(time_, concentration5, RNA20, JGain4);
  Bioenvironment_Degradation_RNA2: Bioenvironment_Degradation_BugbusterRNA2__Bioenvironment_Degradation_BugbusterRNA2(concentration5, kDecay4, J10);
  Bugbuster_RBS_GFP: Bugbuster_RBS_GFP__Bugbuster_RBS_GFP(cellVolume, concentration5, RNARiPs1, k7);
  Bugbuster_ProteinCDS_GFP: Bugbuster_ProteinCDS_GFP__Bugbuster_ProteinCDS_GFP(cellVolume, RNARiPs1, J11);
  GFP: Template_Species__Template_Species(time_, concentration6, GFP0, JGain5);
  Bioenvironment_Degradation_GFP: Bioenvironment_Degradation_GFP__Bioenvironment_Degradation_GFP(concentration6, kDecay5, J12);
end
