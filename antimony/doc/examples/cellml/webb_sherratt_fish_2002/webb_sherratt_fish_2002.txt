//Created by libAntimony v1.4

//Warnings from automatic translation:
//    The units of "time" ('hour') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "time" ('hour') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "time" ('hour') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "time" ('hour') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "time" ('hour') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "time" ('hour') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "time" ('hour') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.

model webb_model_2002__environment(time_)
end

model webb_model_2002__T(T, k1, Lm, RT, m, time_)

  // Variable initializations:
  T = 500.0;
end

model webb_model_2002__LT(LT_, k2, k3, k4, k5, m, T, Rm, time_)

  // Variable initializations:
  LT_ = 0.0;
end

model webb_model_2002__RT(RT, k6, k7, k8, SL, time_)

  // Variable initializations:
  RT = 1E4;
end

model webb_model_2002__m(m, k1, LT_, Rm, T, time_)

  // Variable initializations:
  m = 500.0;
end

model webb_model_2002__Lm(Lm, k10, k3, k4, k5, m, T, RT, time_)

  // Variable initializations:
  Lm = 1E3;
end

model webb_model_2002__Rm(Rm, k6, k7, k8, SL, time_)

  // Variable initializations:
  Rm = 1E3;
end

model webb_model_2002__SL(SL, k11, k3, k9, m, T, Rm, RT, Lm, LT_, time_)

  // Variable initializations:
  SL = 0.0;
end

model webb_model_2002__model_parameters(k1, k2, k3, k4, k5, k6, k7, k8, k9, k10, k11)

  // Variable initializations:
  k1 = 8.38E-10;
  k2 = 6E-3;
  k3 = 5.9413;
  k4 = 0.35;
  k5 = 2.52E-9;
  k6 = 2.244E3;
  k7 = 0.35;
  k8 = 1.92E10;
  k9 = 87.3E8;
  k10 = 3.11E3;
  k11 = 13.9;
end

model webb_model_2002____main()

  // Sub-modules, and any changes to those submodules:
  environment: webb_model_2002__environment(time_);
  T: webb_model_2002__T(T0, k1, Lm0, RT0, m0, time_);
  LT_: webb_model_2002__LT(LT_0, k2, k3, k4, k5, m0, T0, Rm0, time_);
  RT: webb_model_2002__RT(RT0, k6, k7, k8, SL0, time_);
  m: webb_model_2002__m(m0, k1, LT_0, Rm0, T0, time_);
  Lm: webb_model_2002__Lm(Lm0, k10, k3, k4, k5, m0, T0, RT0, time_);
  Rm: webb_model_2002__Rm(Rm0, k6, k7, k8, SL0, time_);
  SL: webb_model_2002__SL(SL0, k11, k3, k9, m0, T0, Rm0, RT0, Lm0, LT_0, time_);
  model_parameters: webb_model_2002__model_parameters(k1, k2, k3, k4, k5, k6, k7, k8, k9, k10, k11);
end
