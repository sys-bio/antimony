//Created by libAntimony v1.4
model IP3__IP3(t, J_gain_IP3)

  // Rate Rules:
  IP3' = J_gain_IP3;

  // Variable initializations:
  IP3 = 0.0150;
end

model IP3____main()

  // Sub-modules, and any changes to those submodules:
  IP3: IP3__IP3(t, J_gain_IP3);
end
