//Created by libAntimony v1.3
model chassagnole_rais_quentin_fell_mazat_2001_version01__environment(time_)
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__Asp(Asp, vAKI, vAKIII, time_)

  // Rate Rules:
  Asp' = vAKI + vAKIII;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__aspp(aspp, vAKI, vAKIII, vASD, time_)

  // Rate Rules:
  aspp' = vAKI + vAKIII + vASD;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__ASA(ASA, vASD, vHDH, time_)

  // Rate Rules:
  ASA' = vASD + vHDH;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__hs(hs, vHDH, vHK, time_)

  // Rate Rules:
  hs' = vHDH + vHK;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__hsp(hsp, vTS, time_)

  // Rate Rules:
  hsp' = vTS;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__Thr(Thr, vAKI, vHDH, vHK, time_)

  // Rate Rules:
  Thr' = vAKI + vHDH + vHK;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__ATP(ATP, vAKI, vAKIII, vHK, time_)

  // Rate Rules:
  ATP' = vAKI + vAKIII + vHK;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__ADP(ADP, vAKI, vAKIII, time_)

  // Rate Rules:
  ADP' = vAKI + vAKIII;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__NADPH(NADPH, vASD, vHDH, time_)

  // Rate Rules:
  NADPH' = vASD + vHDH;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__NADP(NADP, vASD, vHDH, time_)

  // Rate Rules:
  NADP' = vASD + vHDH;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__Pi(Pi_, vASD, time_)

  // Rate Rules:
  Pi_' = vASD;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__Lys(Lys, vAKIII, vHK, time_)

  // Rate Rules:
  Lys' = vAKIII + vHK;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__vAKI(vAKI, Asp, aspp, ATP, ADP, Thr)

  // Assignment Rules:
  vAKI := V_max_AK_I * ( Asp * ATP - aspp * ADP / K_eq ) / (( K_asp * (( 1.0 + power( Thr / K_iThr , h_Thr )) / ( 1.0 + power( Thr / ( K_iThr * alpha ), h_Thr ))) + aspp * ( K_asp / K_aspp ) + Asp ) * ( K_ATP * ( 1.0 + ADP / K_ADP ) + ATP ));

  // Variable initializations:
  K_asp = 0.97;
  K_ATP = 0.98;
  K_aspp = 0.017;
  K_ADP = 0.25;
  K_iThr = 0.167;
  alpha = 2.47;
  h_Thr = 4.09;
  K_eq = 0.00064;
  V_max_AK_I = 463.00;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__vASD(vASD, aspp, ASA, NADPH, NADP, Pi_)

  // Assignment Rules:
  vASD := V_max_ASD * ( aspp * NADPH - ASA * NADP * Pi_ / K_eq ) / (( K_aspp * ( 1.0 + ASA / K_ASA ) * ( 1.0 + Pi_ / K_Pi ) + aspp ) * ( K_NADPH * ( 1.0 + NADP / K_NADP ) + NADPH ));

  // Variable initializations:
  K_aspp = 0.022;
  K_NADPH = 0.029;
  K_ASA = 0.11;
  K_NADP = 0.144;
  K_Pi = 10.2;
  K_eq = 284000.0;
  V_max_ASD = 598.0;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__vHDH(vHDH, ASA, hs, NADPH, NADP, Thr)

  // Assignment Rules:
  vHDH := V_max_HDH * ( ASA * NADPH - hs * NADP / K_eq ) / (( 1.0 + power( Thr / K_iThr , h )) / ( 1.0 + power( Thr / ( alpha * K_iThr ), h )) * ( K_ASA + hs * ( K_ASA / K_hs ) + ASA ) * ( K_NADPH * ( 1.0 + NADP / K_NADP ) + NADPH ));

  // Variable initializations:
  K_ASA = 0.24;
  K_NADPH = 0.037;
  K_hs = 3.39;
  K_NADP = 0.067;
  K_iThr = 0.097;
  alpha = 3.93;
  h = 1.41;
  K_eq = 100000000000.0;
  V_max_HDH = 2585.00;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__vHK(vHK, hs, ATP, Thr, Lys)

  // Assignment Rules:
  vHK := V_max_HK * hs * ATP / (( K_hs * (( 1.0 + ATP / K_iATP ) / ( 1.0 + Thr / K_iThr )) + hs ) * (( K_ATP * ( 1.0 + hs / K_ihs ) + ATP ) * ( 1.0 + Lys / K_iLys )));

  // Variable initializations:
  K_hs = 0.11;
  K_ATP = 0.072;
  K_iThr = 1.09;
  K_iLys = 9.45;
  K_ihs = 4.7;
  K_iATP = 4.35;
  V_max_HK = 483.0;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__vTS(vTS, hsp)

  // Assignment Rules:
  vTS := V_max_TS * hsp / ( K_hsp + hsp );

  // Variable initializations:
  K_hsp = 0.31;
  V_max_TS = 208.0;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01__vAKIII(vAKIII, Asp, aspp, ATP, ADP, Lys)

  // Assignment Rules:
  vAKIII := V_max_AK_III * ( Asp * ATP - aspp * ADP / K_eq ) / (( 1.0 + power( Lys / K_iLys , h_Lys )) * ( K_asp * ( 1.0 + aspp / K_aspp ) + Asp ) * ( K_ATP * ( 1.0 + ADP / K_ADP ) + ATP ));

  // Variable initializations:
  K_asp = 0.32;
  K_ATP = 0.22;
  K_aspp = 0.017;
  K_ADP = 0.25;
  K_iLys = 0.391;
  h_Lys = 2.8;
  K_eq = 0.00064;
  V_max_AK_III = 299.0;
end

model chassagnole_rais_quentin_fell_mazat_2001_version01____main()

  // Sub-modules, and any changes to those submodules:
  environment: chassagnole_rais_quentin_fell_mazat_2001_version01__environment(time_);
  Asp: chassagnole_rais_quentin_fell_mazat_2001_version01__Asp(Asp0, vAKI0, vAKIII0, time_);
  aspp: chassagnole_rais_quentin_fell_mazat_2001_version01__aspp(aspp0, vAKI0, vAKIII0, vASD0, time_);
  ASA: chassagnole_rais_quentin_fell_mazat_2001_version01__ASA(ASA0, vASD0, vHDH0, time_);
  hs: chassagnole_rais_quentin_fell_mazat_2001_version01__hs(hs0, vHDH0, vHK0, time_);
  hsp: chassagnole_rais_quentin_fell_mazat_2001_version01__hsp(hsp0, vTS0, time_);
  Thr: chassagnole_rais_quentin_fell_mazat_2001_version01__Thr(Thr0, vAKI0, vHDH0, vHK0, time_);
  ATP: chassagnole_rais_quentin_fell_mazat_2001_version01__ATP(ATP0, vAKI0, vAKIII0, vHK0, time_);
  ADP: chassagnole_rais_quentin_fell_mazat_2001_version01__ADP(ADP0, vAKI0, vAKIII0, time_);
  NADPH: chassagnole_rais_quentin_fell_mazat_2001_version01__NADPH(NADPH0, vASD0, vHDH0, time_);
  NADP: chassagnole_rais_quentin_fell_mazat_2001_version01__NADP(NADP0, vASD0, vHDH0, time_);
  Pi_: chassagnole_rais_quentin_fell_mazat_2001_version01__Pi(Pi_0, vASD0, time_);
  Lys: chassagnole_rais_quentin_fell_mazat_2001_version01__Lys(Lys0, vAKIII0, vHK0, time_);
  vAKI: chassagnole_rais_quentin_fell_mazat_2001_version01__vAKI(vAKI0, Asp0, aspp0, ATP0, ADP0, Thr0);
  vASD: chassagnole_rais_quentin_fell_mazat_2001_version01__vASD(vASD0, aspp0, ASA0, NADPH0, NADP0, Pi_0);
  vHDH: chassagnole_rais_quentin_fell_mazat_2001_version01__vHDH(vHDH0, ASA0, hs0, NADPH0, NADP0, Thr0);
  vHK: chassagnole_rais_quentin_fell_mazat_2001_version01__vHK(vHK0, hs0, ATP0, Thr0, Lys0);
  vTS: chassagnole_rais_quentin_fell_mazat_2001_version01__vTS(vTS0, hsp0);
  vAKIII: chassagnole_rais_quentin_fell_mazat_2001_version01__vAKIII(vAKIII0, Asp0, aspp0, ATP0, ADP0, Lys0);
end
