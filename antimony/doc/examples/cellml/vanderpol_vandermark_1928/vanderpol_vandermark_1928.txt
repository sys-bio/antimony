//Created by libAntimony v1.3
model vanderpol_vandermark_1928_version01__Main()

  // Rate Rules:
  x' = y * 1;
  y' = (epsilon * (1 - power(x, 2 )) * y - x) / 1;

  // Variable initializations:
  x = -2;
  y = 0;
  epsilon = 1;
end

model vanderpol_vandermark_1928_version01____main()

  // Sub-modules, and any changes to those submodules:
  Main: vanderpol_vandermark_1928_version01__Main();
end
