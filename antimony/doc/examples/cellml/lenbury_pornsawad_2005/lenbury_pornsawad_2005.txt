//Created by libAntimony v1.4

//Warnings from automatic translation:
//    The units of "time" ('dimensionless') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "time" ('dimensionless') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.
//    The units of "time" ('dimensionless') do not have 'seconds' as their base unit, so assuming this CellML model is trying to take the derivative of something with respect to some not-time element, we are not translating this derivative.

model lenbury_2005__environment(time_)
end

model lenbury_2005__x(x, y, beta, delta1, time_)

  // Variable initializations:
  x = 1.0;
end

model lenbury_2005__y(y, x, z, beta, delta2, time_)

  // Variable initializations:
  y = 0.01;
end

model lenbury_2005__z(z, y, delta3, time_)

  // Variable initializations:
  z = 0.01;
end

model lenbury_2005__model_parameters(delta1, delta2, delta3, beta)

  // Variable initializations:
  delta1 = 0.5;
  delta2 = 0.38;
  delta3 = 0.6;
  beta = 1.091;
end

model lenbury_2005____main()

  // Sub-modules, and any changes to those submodules:
  environment: lenbury_2005__environment(time_);
  x: lenbury_2005__x(x0, y0, beta, delta1, time_);
  y: lenbury_2005__y(y0, x0, z0, beta, delta2, time_);
  z: lenbury_2005__z(z0, y0, delta3, time_);
  model_parameters: lenbury_2005__model_parameters(delta1, delta2, delta3, beta);
end
