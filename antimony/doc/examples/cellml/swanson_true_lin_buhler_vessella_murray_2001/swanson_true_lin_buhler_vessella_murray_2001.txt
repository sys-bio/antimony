//Created by libAntimony v1.3
model swanson_2001__environment(time_)
end

model swanson_2001__serum_PSA_dynamics(time_)

  // Assignment Rules:
  Vc := Vo * exp( rho * time_ );

  // Rate Rules:
  p' = 1 * beta_h * Vh + 1 * beta_c * Vc - gamma * p;

  // Variable initializations:
  p = 0.0;
  Vo = 20.0;
  Vh = 0.0;
  beta_h = 0.0;
  beta_c = 1.7210;
  gamma = 1.2896;
  rho = 0.0655;
end

model swanson_2001____main()

  // Sub-modules, and any changes to those submodules:
  environment: swanson_2001__environment(time_);
  serum_PSA_dynamics: swanson_2001__serum_PSA_dynamics(time_);
end
