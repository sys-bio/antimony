//Created by libAntimony v1.3
model ElectrEcoBluModel_SVBM_NoFeedback__SpeciesInitialConcentrations(RNA_Device1, TF, TFS, RNA_Device2, IC, PhzS, PhzM, PYO)

  // Variable initializations:
  RNA_Device1 = 0;
  TF = 8621;
  TFS = 0;
  RNA_Device2 = 0;
  IC = 0;
  PhzS = 0;
  PhzM = 0;
  PYO = 0;
end

model ElectrEcoBluModel_SVBM_NoFeedback__RNA_Device1_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model ElectrEcoBluModel_SVBM_NoFeedback__TF_interface(JPlusProductionDevice1, JMinusDegradation, JMinusTFSFormation, JGain)

  // Assignment Rules:
  JGain := JPlusProductionDevice1 - JMinusDegradation - JMinusTFSFormation;
end

model ElectrEcoBluModel_SVBM_NoFeedback__S(concentration)

  // Variable initializations:
  concentration = 5000;
end

model ElectrEcoBluModel_SVBM_NoFeedback__TFS_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model ElectrEcoBluModel_SVBM_NoFeedback__RNA_Device2_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model ElectrEcoBluModel_SVBM_NoFeedback__PhzM_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model ElectrEcoBluModel_SVBM_NoFeedback__PCA(concentration)

  // Variable initializations:
  concentration = 2;
end

model ElectrEcoBluModel_SVBM_NoFeedback__IC_interface(JPlusProduction, JGain, JMinusDegradation, JMinusPYOFormation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation - JMinusPYOFormation;
end

model ElectrEcoBluModel_SVBM_NoFeedback__PhzS_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model ElectrEcoBluModel_SVBM_NoFeedback__PYO_interface(JPlusProduction, JGain, JMinusDegradation)

  // Assignment Rules:
  JGain := JPlusProduction - JMinusDegradation;
end

model Template_Chassis_WellStirredBag__WellStirredBag(bagVolume)
end

model Chassis_EColi__Chassis_EColi(cellVolume)

  // Sub-modules, and any changes to those submodules:
  WellStirredBag: Template_Chassis_WellStirredBag__WellStirredBag(cellVolume);

  // Variable initializations:
  cellVolume = 1;
end

model Time__Time(time_)
end

model Template_Promoter_Constitutive__Template_Promoter_Constitutive(JRNA, localVolume, k)

  // Assignment Rules:
  JRNA := k * 1e9 / (localVolume * 1e-15 * avogadrosConstant);

  // Variable initializations:
  avogadrosConstant = 6.0221415e23;
end

model ElectrEcoBluDevice1_Promoter__ElectrEcoBluDevice1_Promoter(localVolume, k, JRNA)

  // Sub-modules, and any changes to those submodules:
  Template_Promoter_Constitutive: Template_Promoter_Constitutive__Template_Promoter_Constitutive(JRNA, localVolume, k);

  // Variable initializations:
  k = 1.085;
end

model Template_Species__Template_Species(time_, concentration, concentrationInitialValue, JGain)

  // Rate Rules:
  concentration' = JGain;

  // Variable initializations:
  concentration = concentrationInitialValue;
end

model Template_RxR1P1__Template_RxR1P1(concentration, k, J)

  // Assignment Rules:
  J := k * concentration;
end

model Bioenvironment_Degradation_ElectrEcoBluDevice1RNA__Bioenvironment_Degradation_ElectrEcoBluDevice1RNA(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 0.05;
end

model Template_RBS__Template_RBS(RNA, RNARiPs, k, localVolume)

  // Assignment Rules:
  RNARiPs := 1e9 * k * RNA * localVolume * 1e-15;
end

model ElectrEcoBluDevice1_RBS__ElectrEcoBluDevice1_RBS(localVolume, RNA, RNARiPs, k)

  // Sub-modules, and any changes to those submodules:
  Template_RBS: Template_RBS__Template_RBS(RNA, RNARiPs, k, localVolume);

  // Variable initializations:
  k = 1.4;
end

model Template_ProteinCDS__Template_ProteinCDS(localVolume, hostRNARiPs, J)

  // Assignment Rules:
  J := 1e6 * hostRNARiPs / localVolume;
end

model ElectrEcoBluDevice1_ProteinCDS_TF__ElectrEcoBluDevice1_ProteinCDS_TF(localVolume, hostRNARiPs, J)

  // Sub-modules, and any changes to those submodules:
  Template_ProteinCDS: Template_ProteinCDS__Template_ProteinCDS(localVolume, hostRNARiPs, J);
end

model Bioenvironment_Degradation_TF__Bioenvironment_Degradation_TF(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 5.8e-3;
end

model Template_RxR2P1Rev__Template_RxR2P1Rev(J, reactant1, reactant2, kf, kb, product)

  // Assignment Rules:
  J := reactant1 * reactant2 * kf - kb * product;
end

model Bioenvironment_RxR2P1Rev_TFSFormation__Bioenvironment_RxR2P1Rev_TFSFormation(J, reactant1, reactant2, kf, kb, product)

  // Sub-modules, and any changes to those submodules:
  Template_RxR2P1Rev: Template_RxR2P1Rev__Template_RxR2P1Rev(J, reactant1, reactant2, kf, kb, product);

  // Variable initializations:
  kf = 1e3;
  kb = 4e6;
end

model Bioenvironment_Degradation_TFS__Bioenvironment_Degradation_TFS(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 3.851e-4;
end

model Template_Promoter_Inductive__Template_Promoter_Inductive(JRNA, localVolume, k, tFactor, n, Km)

  // Assignment Rules:
  JRNA := k * power(tFactor, n) / (power(Km, n) + power(tFactor, n)) * 1e9 / (localVolume * 1e-15 * avogadrosConstant);

  // Variable initializations:
  avogadrosConstant = 6.0221415e23;
end

model ElectrEcoBluDevice2_Promoter__ElectrEcoBluDevice2_Promoter(JRNA, localVolume, k, tFactor, Km, n)

  // Sub-modules, and any changes to those submodules:
  Template_Promoter_Inductive: Template_Promoter_Inductive__Template_Promoter_Inductive(JRNA, localVolume, k, tFactor, n, Km);

  // Variable initializations:
  k = 89;
  Km = 15000;
  n = 1;
end

model Bioenvironment_Degradation_ElectrEcoBluDevice2RNA__Bioenvironment_Degradation_ElectrEcoBluDevice2RNA(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 8e-6;
end

model ElectrEcoBluDevice2_RBS_PhzM__ElectrEcoBluDevice2_RBS_PhzM(localVolume, RNA, RNARiPs, k)

  // Sub-modules, and any changes to those submodules:
  Template_RBS: Template_RBS__Template_RBS(RNA, RNARiPs, k, localVolume);

  // Variable initializations:
  k = 40;
end

model ElectrEcoBluDevice2_ProteinCDS_PhzM__ElectrEcoBluDevice2_ProteinCDS_PhzM(localVolume, hostRNARiPs, J)

  // Sub-modules, and any changes to those submodules:
  Template_ProteinCDS: Template_ProteinCDS__Template_ProteinCDS(localVolume, hostRNARiPs, J);
end

model ElectrEcoBluDevice2_RBS_PhzS__ElectrEcoBluDevice2_RBS_PhzS(localVolume, RNA, RNARiPs, k)

  // Sub-modules, and any changes to those submodules:
  Template_RBS: Template_RBS__Template_RBS(RNA, RNARiPs, k, localVolume);

  // Variable initializations:
  k = 1;
end

model ElectrEcoBluDevice2_ProteinCDS_PhzS__ElectrEcoBluDevice2_ProteinCDS_PhzS(localVolume, hostRNARiPs, J)

  // Sub-modules, and any changes to those submodules:
  Template_ProteinCDS: Template_ProteinCDS__Template_ProteinCDS(localVolume, hostRNARiPs, J);
end

model Bioenvironment_Degradation_PhzM__Bioenvironment_Degradation_PhzM(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 8e-1;
end

model Template_RxR1P1MM__Template_RxR1P1MM(J, reactant, catalyst, k, Km)

  // Assignment Rules:
  J := k * catalyst * reactant / (reactant + Km);
end

model Bioenvironment_RxR1P1MM_ICFormation__Bioenvironment_RxR1P1MM_ICFormation(J, reactant, catalyst, k, Km)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1MM: Template_RxR1P1MM__Template_RxR1P1MM(J, reactant, catalyst, k, Km);

  // Variable initializations:
  k = 0.2;
  Km = 0.1;
end

model Bioenvironment_Degradation_IC__Bioenvironment_Degradation_IC(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 5e-5;
end

model Bioenvironment_Degradation_PhzS__Bioenvironment_Degradation_PhzS(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 8e-1;
end

model Bioenvironment_RxR1P1MM_PYOFormation__Bioenvironment_RxR1P1MM_PYOFormation(J, reactant, catalyst, k, Km)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1MM: Template_RxR1P1MM__Template_RxR1P1MM(J, reactant, catalyst, k, Km);

  // Variable initializations:
  k = 1;
  Km = 0.1;
end

model Bioenvironment_Degradation_PYO__Bioenvironment_Degradation_PYO(concentration, kDecay, J)

  // Sub-modules, and any changes to those submodules:
  Template_RxR1P1: Template_RxR1P1__Template_RxR1P1(concentration, kDecay, J);

  // Variable initializations:
  kDecay = 5e-1;
end

model ElectrEcoBluModel_SVBM_NoFeedback____main()

  // Sub-modules, and any changes to those submodules:
  SpeciesInitialConcentrations: ElectrEcoBluModel_SVBM_NoFeedback__SpeciesInitialConcentrations(RNA_Device10, TF0, TFS0, RNA_Device20, IC0, PhzS0, PhzM0, PYO0);
  RNA_Device1_interface: ElectrEcoBluModel_SVBM_NoFeedback__RNA_Device1_interface(JRNA, JGain, J);
  TF_interface: ElectrEcoBluModel_SVBM_NoFeedback__TF_interface(J0, J1, J2, JGain0);
  S: ElectrEcoBluModel_SVBM_NoFeedback__S(concentration1);
  TFS_interface: ElectrEcoBluModel_SVBM_NoFeedback__TFS_interface(J2, JGain1, J3);
  RNA_Device2_interface: ElectrEcoBluModel_SVBM_NoFeedback__RNA_Device2_interface(JRNA0, JGain2, J4);
  PhzM_interface: ElectrEcoBluModel_SVBM_NoFeedback__PhzM_interface(J5, JGain3, J6);
  PCA: ElectrEcoBluModel_SVBM_NoFeedback__PCA(concentration5);
  IC_interface: ElectrEcoBluModel_SVBM_NoFeedback__IC_interface(J8, JGain4, J7, J9);
  PhzS_interface: ElectrEcoBluModel_SVBM_NoFeedback__PhzS_interface(J11, JGain5, J10);
  PYO_interface: ElectrEcoBluModel_SVBM_NoFeedback__PYO_interface(J9, JGain6, J12);
  eColiCell: Chassis_EColi__Chassis_EColi(cellVolume);
  Time_: Time__Time(time_);
  ElectrEcoBluDevice1_Promoter: ElectrEcoBluDevice1_Promoter__ElectrEcoBluDevice1_Promoter(cellVolume, k, JRNA);
  RNA_Device1: Template_Species__Template_Species(time_, concentration, RNA_Device10, JGain);
  Bioenvironment_Degradation_Device1RNA: Bioenvironment_Degradation_ElectrEcoBluDevice1RNA__Bioenvironment_Degradation_ElectrEcoBluDevice1RNA(concentration, kDecay, J);
  ElectrEcoBluDevice1_RBS: ElectrEcoBluDevice1_RBS__ElectrEcoBluDevice1_RBS(cellVolume, concentration, RNARiPs, k0);
  ElectrEcoBluDevice1_ProteinCDS_TF: ElectrEcoBluDevice1_ProteinCDS_TF__ElectrEcoBluDevice1_ProteinCDS_TF(cellVolume, RNARiPs, J0);
  TF: Template_Species__Template_Species(time_, concentration0, TF0, JGain0);
  Bioenvironment_Degradation_TF: Bioenvironment_Degradation_TF__Bioenvironment_Degradation_TF(concentration0, kDecay0, J1);
  RxR2P1_TFSformation: Bioenvironment_RxR2P1Rev_TFSFormation__Bioenvironment_RxR2P1Rev_TFSFormation(J2, concentration0, concentration1, kf, kb, concentration2);
  TFS: Template_Species__Template_Species(time_, concentration2, TFS0, JGain1);
  Bioenvironment_Degradation_TFS: Bioenvironment_Degradation_TFS__Bioenvironment_Degradation_TFS(concentration2, kDecay1, J3);
  ElectrEcoBluDevice2_Promoter: ElectrEcoBluDevice2_Promoter__ElectrEcoBluDevice2_Promoter(JRNA0, cellVolume, k1, concentration2, Km, n);
  RNA_Device2: Template_Species__Template_Species(time_, concentration3, RNA_Device20, JGain2);
  Bioenvironment_Degradation_Device2RNA: Bioenvironment_Degradation_ElectrEcoBluDevice2RNA__Bioenvironment_Degradation_ElectrEcoBluDevice2RNA(concentration3, kDecay2, J4);
  ElectrEcoBluDevice2_RBS_PhzM: ElectrEcoBluDevice2_RBS_PhzM__ElectrEcoBluDevice2_RBS_PhzM(cellVolume, concentration3, RNARiPs0, k2);
  ElectrEcoBluDevice2_ProteinCDS_PhzM: ElectrEcoBluDevice2_ProteinCDS_PhzM__ElectrEcoBluDevice2_ProteinCDS_PhzM(cellVolume, RNARiPs0, J5);
  ElectrEcoBluDevice2_RBS_PhzS: ElectrEcoBluDevice2_RBS_PhzS__ElectrEcoBluDevice2_RBS_PhzS(cellVolume, concentration3, RNARiPs1, k3);
  ElectrEcoBluDevice2_ProteinCDS_PhzS: ElectrEcoBluDevice2_ProteinCDS_PhzS__ElectrEcoBluDevice2_ProteinCDS_PhzS(cellVolume, RNARiPs1, J11);
  PhzM: Template_Species__Template_Species(time_, concentration4, PhzM0, JGain3);
  Bioenvironment_Degradation_PhzM: Bioenvironment_Degradation_PhzM__Bioenvironment_Degradation_PhzM(concentration4, kDecay3, J6);
  RxR1P1MM_ICformation: Bioenvironment_RxR1P1MM_ICFormation__Bioenvironment_RxR1P1MM_ICFormation(J8, concentration5, concentration4, k4, Km0);
  IC: Template_Species__Template_Species(time_, concentration6, IC0, JGain4);
  Bioenvironment_Degradation_IC: Bioenvironment_Degradation_IC__Bioenvironment_Degradation_IC(concentration6, kDecay4, J7);
  PhzS: Template_Species__Template_Species(time_, concentration7, PhzS0, JGain5);
  Bioenvironment_Degradation_PhzS: Bioenvironment_Degradation_PhzS__Bioenvironment_Degradation_PhzS(concentration7, kDecay5, J10);
  RxR1P1MM_PYOformation: Bioenvironment_RxR1P1MM_PYOFormation__Bioenvironment_RxR1P1MM_PYOFormation(J9, concentration6, concentration7, k5, Km1);
  PYO: Template_Species__Template_Species(time_, concentration8, PYO0, JGain6);
  Bioenvironment_Degradation_PYO: Bioenvironment_Degradation_PYO__Bioenvironment_Degradation_PYO(concentration8, kDecay6, J12);
end
