//Created by libAntimony v1.3
model bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__environment(time_)
end

model bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__T(T, b, I, time_)

  // Rate Rules:
  T' = s - ( dT * T + b * T * I );

  // Variable initializations:
  s = 10.0;
  dT = 0.01;
end

model bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__I(I, b, T, E, time_)

  // Rate Rules:
  I' = b * T * I - ( dI * I + p * I * E );

  // Variable initializations:
  p = 0.05;
  dI = 0.3;
end

model bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__E(E, I, time_)

  // Rate Rules:
  E' = c * I - dE * E;

  // Variable initializations:
  c = 0.3;
  dE = 0.1;
end

model bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__kinetic_parameters(b)

  // Variable initializations:
  b = 0.001;
end

model bonhoeffer_rembiszewski_ortiz_nixon_2000_version03____main()

  // Sub-modules, and any changes to those submodules:
  environment: bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__environment(time_);
  T: bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__T(T0, b, I0, time_);
  I: bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__I(I0, b, T0, E0, time_);
  E: bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__E(E0, I0, time_);
  kinetic_parameters: bonhoeffer_rembiszewski_ortiz_nixon_2000_version03__kinetic_parameters(b);
end
