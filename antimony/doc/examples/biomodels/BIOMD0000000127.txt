model model_0000001()

  // Compartments and Species:
  compartment cell;

  // Rate Rules:
  v' = 0.04 * pow(v, 2) + 5 * v + 140 - U + i
  U' = a * (b * v - U)

  // Events:
  event_0000001: @gt(v, Vthresh): U = U + d: v = c;
  event_0000002: @gt(time, 10): i = 10;

  // Compartments initializations:
  cell = 1;

  // Other initializations:
  Vthresh = 30;
  c = -65;
  d = 8;
  i = 0;
  a = 0.02;
  b = 0.2;

  var v, U, i;
end
